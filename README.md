# enforcer-m4-trusted

Disposable fixture for StandardsEnforcer M4 live gate validation. Hosts the trusted reusable
workflow that governed repositories call, pinned to a commit SHA.

Not for reuse. See `StandardsEnforcer/artifacts/evidence/` for what this was used to establish.
